# Humanitarian Consultants Candidates Form

 # Install and Run Project
 -   Run `npm install --force` to install dependencies with no dependencies error.
 -   Run `npm start` to view the web page locally
